"use strict";
function render(document) {
    if (typeof document === 'string') {
        console.log(document);
    }
    else if (typeof document === 'number') {
        console.log(document);
    }
}
